import React from 'react';

export const DashboardLayout: React.FC<{ children: React.ReactNode }> = ({ children    }) => (
  <div className=" ">$2</div><div className="{children}">$2</div>
    </div>);

export default DashboardLayout;

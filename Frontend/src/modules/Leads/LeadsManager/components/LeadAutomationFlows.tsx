import React, { useState } from 'react';
import Card from '@/shared/components/ui/Card';

export const LeadAutomationFlows: React.FC = () => {
  const [flows] = useState([]);

  return (
            <div className=" ">$2</div><h2 className="text-xl font-bold mb-4">Automation Flows</h2>
      <Card className="p-4" />
        <p className="text-gray-500">No flows configured</p></Card></div>);};

export default LeadAutomationFlows;
